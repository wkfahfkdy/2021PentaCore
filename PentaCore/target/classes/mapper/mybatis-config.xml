<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
  <configuration>
  		<!-- 카멜케이스 ture = first_name >> firstName 으로 변경 -->
  		<settings>
  			<setting name="mapUnderscoreToCamelCase" value="false"/>
  		</settings>
  		<!-- Alias 설정 -->
  		<typeAliases>
  			<typeAlias type="com.yedam.storage.storage.vo.StorageVO" alias="StorageVO"/>
  		</typeAliases>
  </configuration>